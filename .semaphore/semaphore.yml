version: v1.0
name: Semaphore Python/Flask/Docker Pipeline

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004

branches:
  - main  # or master

blocks:
  - name: Build
    task:
      secrets:
        - name: pyflask-semaphore
      jobs:
        - name: Docker build
          commands:
            - echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
            - checkout
            - docker-compose build
            - docker tag pyflasksemaphore:latest "$DOCKER_USERNAME/pyflasksemaphore:latest"
            - docker tag pyflasksemaphore:latest "$DOCKER_USERNAME/pyflasksemaphore:$SEMAPHORE_WORKFLOW_ID"
            - docker push "$DOCKER_USERNAME/pyflasksemaphore:latest"
            - docker push "$DOCKER_USERNAME/pyflasksemaphore:$SEMAPHORE_WORKFLOW_ID"
            - docker images

  - name: Run & Test
    task:
      secrets:
        - name: pyflask-semaphore
      prologue:
        commands:
          - echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
          - checkout
          - docker pull "$DOCKER_USERNAME/pyflasksemaphore"
          - docker-compose up -d
          - timeout 120 bash -c 'while [[ "$(docker inspect -f "{{.State.Health.Status}}" semaphore-pyflask-docker_flasksemaphore_1)" != "healthy" ]]; do sleep 5; echo "Waiting..."; done'
      jobs:
        - name: Check Containers
          commands:
            - docker ps -a
            - docker inspect semaphore-pyflask-docker_flasksemaphore_1
            - docker-compose logs --tail=20

        - name: Run Tests
          commands:
            - docker exec semaphore-pyflask-docker_flasksemaphore_1 python -m unittest discover -s /opt -p "test_*.py" -v || exit 1
      epilogue:
        commands:
          - docker-compose down -v

promotions:
  - name: Deploy to Heroku
    pipeline_file: deploy-heroku.yml